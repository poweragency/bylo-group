---
const currentLocale = (Astro.currentLocale ?? 'it') as 'it' | 'en';
const isEN = currentLocale === 'en';

const t = {
  it: {
    servizi: 'Servizi',
    marche: 'Marche',
    zone: 'Zone',
    auto: 'Le nostre auto',
    contatti: 'Contatti',
    cta: 'Prenota un servizio',
    home: 'Tommy By — home',
    menuLabel: 'Apri menu',
    switchLabel: 'English',
  },
  en: {
    servizi: 'Services',
    marche: 'Brands',
    zone: 'Zones',
    auto: 'Our cars',
    contatti: 'Contact',
    cta: 'Book a service',
    home: 'Tommy By — home',
    menuLabel: 'Open menu',
    switchLabel: 'Italiano',
  },
}[currentLocale];

const base = isEN ? '/en' : '';
const sectionPath = isEN ? {
  servizi: '/en/services/',
  marche: '/en/brands/',
  zone: '/en/zones/',
  auto: '/en/auto/',
} : {
  servizi: '/servizi/',
  marche: '/marche/',
  zone: '/zone/',
  auto: '/auto/',
};
const contattiAnchor = `${base}/#contatti`;

const nav = [
  { href: sectionPath.servizi, label: t.servizi },
  { href: sectionPath.marche, label: t.marche },
  { href: sectionPath.zone, label: t.zone },
  { href: sectionPath.auto, label: t.auto },
  { href: contattiAnchor, label: t.contatti },
];

const switchHref = isEN
  ? Astro.url.pathname.replace(/^\/en(\/|$)/, '/')
  : `/en${Astro.url.pathname === '/' ? '' : Astro.url.pathname}`;
---

<header
  class="sticky top-0 z-50 bg-byl-white border-b border-byl-stone text-byl-black"
>
  <div class="container-page flex items-center justify-between h-16 md:h-20">
    <a href={base || '/'} class="flex items-center" aria-label={t.home}>
      <img
        src="/logo.png"
        alt="BYLO Group"
        width="800"
        height="316"
        loading="eager"
        fetchpriority="high"
        decoding="async"
        class="h-10 md:h-12 w-auto"
      />
    </a>

    <nav class="hidden lg:flex items-center gap-8">
      {
        nav.map((item) => (
          <a
            href={item.href}
            class="text-sm font-medium text-byl-black hover:text-byl-yellow-dark transition-colors"
          >
            {item.label}
          </a>
        ))
      }
    </nav>

    <div class="flex items-center gap-3">
      <a
        href={switchHref}
        class="text-xs uppercase tracking-wider font-medium text-byl-mute hover:text-byl-black transition-colors"
        aria-label={t.switchLabel}
      >
        {isEN ? 'IT' : 'EN'}
      </a>
      <a href={contattiAnchor} class="btn-primary hidden sm:inline-flex text-sm">
        {t.cta}
      </a>
      <button
        id="mobile-menu-btn"
        class="lg:hidden -mr-2 p-2.5 text-byl-black rounded-md hover:bg-byl-off transition-colors"
        aria-label={t.menuLabel}
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
      </button>
    </div>
  </div>

  <div id="mobile-menu" class="hidden lg:hidden border-t border-byl-stone bg-byl-white">
    <nav class="container-page py-4 flex flex-col gap-1">
      {
        nav.map((item) => (
          <a
            href={item.href}
            class="py-3 text-base font-medium text-byl-black hover:text-byl-yellow-dark"
          >
            {item.label}
          </a>
        ))
      }
      <a href={switchHref} class="py-3 text-base font-medium text-byl-mute hover:text-byl-black">
        {t.switchLabel}
      </a>
      <a href={contattiAnchor} class="btn-primary mt-3 text-sm w-full">
        {t.cta}
      </a>
    </nav>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn?.addEventListener('click', () => {
    const isHidden = menu?.classList.toggle('hidden');
    btn.setAttribute('aria-expanded', String(isHidden === false));
  });
</script>
